Hand-Glide is made via Mediapipe library on python. 
It detects hand gestures in realtime and processes the output as per its landmarks.

Requirements:
• Python 3.7 (Or above)
• PyAutoGui
• OpenCV
• Mediapipe
